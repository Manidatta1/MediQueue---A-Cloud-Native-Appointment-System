namespace: healthcare

global:
  db:
    host: postgres-service
    user: healthcare_admin
    password: healthcareadmin123
    name: healthcare
  redisHost: redis-service-master
  rabbitHost: rabbitmq-service
  jwtSecret: xyz
  jwtAlgorithm: HS256

# ---------------- BACKEND ----------------
backend:
  image: "us-central1-docker.pkg.dev/healthcare-platform-477020/healthcare-repo/backend:latest"
  replicas: 2
  port: 8000
  service:
    type: ClusterIP          # 👈 internal only (no external IP)

# ---------------- AUTH SERVICE ----------------
auth:
  image: "us-central1-docker.pkg.dev/healthcare-platform-477020/healthcare-repo/auth:latest"
  replicas: 2
  port: 8001
  service:
    type: ClusterIP          # 👈 internal only (no external IP)

# ---------------- CONSUMER WORKER ----------------
consumer:
  image: "us-central1-docker.pkg.dev/healthcare-platform-477020/healthcare-repo/backend:latest"
  replicas: 1

# ---------------- FRONTEND ----------------
frontend:
  image: "us-central1-docker.pkg.dev/healthcare-platform-477020/healthcare-repo/frontend:latest"
  replicas: 2
  port: 80                 # 👈 since Nginx serves on port 80
  service:
    type: LoadBalancer      # 👈 expose this publicly via GCP load balancer
  ingress:
    enabled: false          # 👈 disable ingress entirely (we’re using LoadBalancer instead)
